<div class="wrapper">
      
      <div id="tag_cloud">
  <% tag_cloud Project.tag_counts, %w[s m l] do |tag, css_class| %>
    <%= link_to tag.name, tag_path(tag.name), class: css_class %>
  <% end %>
</div>

      <!-- Sidebar Start -->
      <nav id="sidebar">
        <div class="sidebar-content">
          <div class="sidebar-header">
            <p>Vivek HV</p>
          </div>

          <ul class="sidebar-menu">
            <li><a href="#">Contact</a></li>
          </ul>
        </div>
      </nav>

      <nav id="sidebar-mobile" class='col-12'>
        <div class="sidebar-content">
          <div class="sidebar-header">
            <p>Vivek HV</p>

            <div id="tag_cloud">
              <% tag_cloud Project.tag_counts, %w[s m l] do |tag, css_class| %>
                <%= link_to tag.name, tag_path(tag.name), class: css_class %>
              <% end %>
            </div>
          </div>


          
          <ul class="sidebar-menu">
            <li><a href="#">Contact</a></li>
          </ul>
        </div>
      </nav>
      <!-- Sidebar End -->

      <!-- Content Start -->
      <div class="content" id="style-3">
        <div class="row1">

    <% @projects.each_with_index do |project, index| %>

          <div class="card">
            <!-- <p class="side-text">Graduate School of Design / Harvard University / Fall 2017</p> -->
            <div class="wrapper-rhs">
              <div class="img-container">
                <img class="img-card" src="<%= project.thumb_img %>" target='vid-<%= index %>' toggle="img-<%= index %>">
                <video class="vid-card"  width="400" height="400" target='img-<%= index %>' toggle="vid-<%= index %>"   muted loop>
                    <source src="<%= project.thumb_vid %>" type="video/mp4">
                    Your browser does not support HTML5 video.
                </video>
              </div>

              <div class="text-wrapper">
                <p class="title"><%= link_to project.title, project %></p>
                <!-- <p class="sub-text-1">#productdesign #spatialdesign #vr #creativity #productdesign #spatialdesign #vr</p> -->
              </div>
            </div>
          </div>
<!--
        <td><%#= link_to 'Edit', edit_project_path(project) %></td>
        <td><%#= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr> -->
    <% end %>








        </div>
      </div>
      <!-- Content End -->

    </div>



<p id="notice"><%= notice %></p>

<h1>Projects</h1>





<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Thumb img</th>
      <th>Thumb vid</th>
      <th>Short desc</th>
      <th>Location</th>
      <th>Period</th>
      <th>Date</th>
      <th>Long desc</th>
      <th>Recognition</th>
      <th>Role</th>
      <th>Views</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.title %></td>
        <td><%= project.thumb_img %></td>
        <td><%= project.thumb_vid %></td>
        <td><%= project.short_desc %></td>
        <td><%= project.location %></td>
        <td><%= project.period %></td>
        <td><%= project.date %></td>
        <td><%= project.long_desc %></td>
        <td><%= project.recognition %></td>
        <td><%= project.role %></td>
        <td><%= project.views %></td>
        <td><%= link_to 'Show', project %></td>
        <td><%= link_to 'Edit', edit_project_path(project) %></td>
        <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Project', new_project_path %>
