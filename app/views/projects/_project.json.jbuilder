json.extract! project, :id, :title, :thumb_img, :thumb_vid, :short_desc, :location, :period, :date, :long_desc, :recognition, :role, :views, :created_at, :updated_at
json.url project_url(project, format: :json)
